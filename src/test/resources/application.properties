# Keep tests hermetic while still exercising the full OpenTelemetry auto-configuration:
# load the SDK but use no-op exporters so nothing tries to reach a collector, which
# still catches wiring/version issues without needing a running collector.
otel.traces.exporter=none
otel.metrics.exporter=none
otel.logs.exporter=none
